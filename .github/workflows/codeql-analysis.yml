name: "CodeQL"

on:
  push:
    branches:
      - main
  pull_request:
  schedule:
    - cron: '56 5 * * 1'

jobs:
  analyze:
    uses: 0-vortex/create-react-app-5-test/.github/workflows/codeql-analysis.yml@main
